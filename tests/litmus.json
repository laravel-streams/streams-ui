{
    "handle": "testing.litmus",
    "description": "Used for testing.",
    
    "source": {
        "path": "vendor/streams/ui/tests/data/litmus",
        "format": "json"
    },

    "fields": {
        "text": {
            "type": "text",
            "label": "Text Input",
            "disabled": true,
            "readonly": true,
            "rules": [
                "required"
            ]
        }
    }
}
