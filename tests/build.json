{
    "handle": "testing.build",
    "description": "Used for testing building streams from JSON file.",
    
    "source": {
        "path": "vendor/streams/ui/tests/data/runtime",
        "format": "json"
    },
    "rules": {
        "id": "required"
    },
    "fields": {
        "name": "text"
    }
}
